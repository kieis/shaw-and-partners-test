import "fastify";

/*declare module "fastify" {
  type RequestData = {
    //user: object | undefined;
    query: {
      q: string | undefined
    }
  };

  export interface FastifyRequest extends RequestData {}
}
*/

